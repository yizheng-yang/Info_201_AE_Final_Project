library(shiny)

# Define UI for application that draws a histogram
shinyUI(fluidPage(
  
  # Application title
  titlePanel("Studio Ghibli"),
  
  # Sidebar
  sidebarLayout(
    
    ),
    
    # Show a plot of the generated distribution
    mainPanel(
       
    )
  )
))
